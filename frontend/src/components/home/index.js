import Navigation from "../Navigation";

const home = (props) => {
    return (
        <div id="homeContainer">
            <Navigation />
            home
        </div>
    );
};

export default home;
